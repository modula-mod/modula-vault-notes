# Legacy manifests

Canonical product source: `modula.product.json` (MPS 1.0-RC).

These files remain for compatibility with already-published Module Standard / Greenfield releases:

- `modula.module.json` — Module Standard 2.1 adapter
- `module.manifest.json` — Greenfield registry adapter

They must not independently change product identity. New behaviour is declared in `modula.product.json` first.

Historical published IDs stay published. Family and aliases present coherent identity without renaming Vault AI/Voice/Formatting nested IDs.
