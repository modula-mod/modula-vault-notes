# Exports

Supported export formats are JSON, Markdown, plain text and ZIP bundle.

Every export includes a machine-readable manifest with module ID, module
version, Standard version, data schema version, export timestamp and record
count. Exports preserve note IDs, timestamps, folder relationships, tags,
internal links, saved-source provenance and document schema version.

Export jobs are bounded and auditable. Export authorization is required.
