# Testing

Run the module tests with:

```sh
pnpm verify
```

Coverage includes manifest validation, version identity, record schemas,
structured document validation, actions, functions, permissions, events,
settings, search declarations, diagnostics, health, fixtures, prohibited
constructs, optimistic concurrency, idempotency, export manifests and
content-redaction boundaries.
