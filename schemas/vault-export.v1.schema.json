{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["manifest", "format"],
  "properties": {
    "format": {"enum": ["json", "markdown", "plain-text", "zip"]},
    "manifest": {"type": "object"}
  }
}
